<!DOCTYPE html>
<html>
<head>
  <%= stylesheet_link_tag 'roots/application', media: 'all' %>
  <title>Roots</title>
</head>
  <body class='container'>
    <div class='col-xs-6 '>
      <input class='form-control' type='text' placeholder='Search for...'/>
    </div>

    <div class='col-xs-12 vertical-spacer'></div>

    <div class ='col-xs-12 '>
      <h2>Engine Routes</h2>
        <% @route_collection.engine_routes.each do |engine_hash| %>
          <%= render partial: 'engine_route', locals: { engine_hash: engine_hash } %>
        <% end %>
      <h2>App Routes</h2>
        <% @route_collection.application_routes.each do |route_hash| %>
          <%= render partial: 'route', locals: { route_hash: route_hash } %>
        <% end %>
    </div>
  </body>
</html>
